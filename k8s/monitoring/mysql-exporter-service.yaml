apiVersion: v1
kind: Service

metadata:
  name: mysql-exporter
  namespace: ecommerce

spec:
  type: ClusterIP

  selector:
    app: mysql-exporter

  ports:
    - name: metrics
      port: 9104
      targetPort: 9104